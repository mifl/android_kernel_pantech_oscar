GCC_VERSION      := $(shell $(CONFIG_SHELL) $(PWD)/scripts/gcc-version.sh $(CROSS_COMPILE)gcc)
EXTRA_CFLAGS += -Idrivers/media/video/msm8x60
EXTRA_CFLAGS += -Idrivers/media/video/msm8x60/io
obj-$(CONFIG_MSM_ACTUATOR) += msm_actuator.o
